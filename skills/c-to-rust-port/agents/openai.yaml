interface:
  display_name: "C to Rust Port"
  short_description: "Manifesto-guided C/C++ to Rust rewrite workflow"
  default_prompt: "Use $c-to-rust-port to drive a rewrites.bio-aligned, tool-evidenced C/C++ to Rust rewrite."
policy:
  allow_implicit_invocation: true
